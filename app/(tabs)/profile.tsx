import { PhasePlaceholder } from '@/components/PhasePlaceholder';

export default function ProfileScreen() {
  return (
    <PhasePlaceholder
      title="Your profile"
      phase="Profile"
      description="Name, phone, KYC verification badge, driver-mode toggle, past rides, and logout."
    />
  );
}
