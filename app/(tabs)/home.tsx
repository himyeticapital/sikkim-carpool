import { PhasePlaceholder } from '@/components/PhasePlaceholder';

export default function HomeScreen() {
  return (
    <PhasePlaceholder
      title="Find a ride"
      phase="Home / Search"
      description="Search by where you're going, see matching rides across Sikkim, and open one to book a seat."
    />
  );
}
